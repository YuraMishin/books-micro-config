spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
      - id: client1
        uri: lb://eclient1
        predicates:
        - Path=/
        - Method=GET
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
